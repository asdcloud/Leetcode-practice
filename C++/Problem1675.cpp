#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int minimumDeviation(vector<int>& nums) {
        int first_max = 0, second_max = 0, first_min = INT_MAX, second_min = INT_MAX;
        for (int a = 0; a < nums.size(); a++) {
            if (nums[a] > first_max) {
                
            }
        }
    }
};

int main() {

}